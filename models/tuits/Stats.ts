/**
 * @file Declares Tuit Stats data type represented by values like replies, retuits, likes and dislikes.
 */
export default interface Stats {
    replies?: number,
    retuits: number,
    likes: number,
    dislikes: Number
};